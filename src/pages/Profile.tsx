import { Mail, MapPin, Phone, RefreshCw } from 'lucide-react';
import { useApp } from '../store/app';
export function Profile() { const user = useApp(s => s.user)!; const initials = user.nombre.split(' ').map(x => x[0]).join('').slice(0, 2).toUpperCase(); return <div className="page"><div className="profile-card"><div className="avatar huge">{initials}</div><h1>{user.nombre}</h1><p>{user.rol}</p><span className="profile-chip">TAS Honduras</span><div className="profile-metrics"><div><strong>0</strong><span>Este mes</span></div><div><strong>0</strong><span>Pendientes</span></div><div><strong>0</strong><span>Total</span></div></div><ul><li><Mail /> <div><small>Correo</small>{user.correo}</div></li><li><Phone /><div><small>Teléfono</small>{user.telefono || 'Sin teléfono'}</div></li><li><MapPin /><div><small>Zona asignada</small>Pendiente de asignar</div></li><li><RefreshCw /><div><small>Sincronización</small>{navigator.onLine ? 'En línea' : 'Trabajando sin conexión'}</div></li></ul></div></div> }
