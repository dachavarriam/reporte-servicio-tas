import { useEffect } from 'react';
import { useApp } from '../store/app';
export function MetricsPage(){ const {metrics,items,load}=useApp(); useEffect(()=>{void load()},[load]); return <div className="page"><div className="page-heading"><h1>Métricas</h1><p>Indicadores operativos del piloto.</p></div><div className="metrics-grid desktop">{[['Total',metrics?.total],['Borradores',metrics?.borradores],['En revisión',metrics?.revision],['Pendientes de firma',metrics?.pendientesFirma],['Completados',metrics?.completados],['Tiempo promedio',`${metrics?.tiempoPromedioDias??0} d`]].map(([a,b])=><div className="metric" key={a}><strong>{b}</strong><span>{a}</span></div>)}</div><section className="detail-section"><h2>Actividad reciente</h2>{items.slice(0,6).map(r=><div className="line-item" key={r.id}><strong>{r.id}</strong><span>{r.cliente} · {r.estado}</span></div>)}</section></div> }
